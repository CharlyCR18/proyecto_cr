import 'package:flutter/material.dart';

class StNotifierProvider extends StatelessWidget {
  const StNotifierProvider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
